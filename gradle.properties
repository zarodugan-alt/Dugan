org.gradle.jvmargs=-Xmx3072m -Dfile.encoding=UTF-8 -XX:+UseParallelGC
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
android.useAndroidX=true
android.nonTransitiveRClass=true
android.defaults.buildfeatures.buildconfig=false
kotlin.code.style=official

# Set to true (or -Pdugan.firebase=true) once you drop a real app/google-services.json in place.
# Left off by default so the project builds without any Firebase project.
dugan.firebase=false
